ultralytics>=8.3
lap>=0.5.12  # ByteTrack's assignment solver; without it ultralytics pip-installs it on every start
opencv-python-headless
paho-mqtt>=2.0
python-ulid>=2.7
typing_extensions  # python-ulid 4.x imports it on py<3.12 without declaring it
