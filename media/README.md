Put a fixed-camera traffic clip here as `sample.mp4` (not committed). It feeds the Grafana demo and the
public GIFs, so use footage whose licence allows republication: self-shot, CC0, CC BY, or a stock licence
such as Pexels. Research datasets (UA-DETRAC, MOT17, AI City Challenge) are for metrics only, never frames.
Log the clip in [SOURCES.md](SOURCES.md).
